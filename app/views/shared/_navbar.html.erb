<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t('navbar.title'), :root, :class => 'navbar-brand' %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(root_url) %>"><%= link_to t('navbar.home'), :root %></li>
        <% if (controller_name.eql? 'mocks') && action_name.in?( %w(show history)) %>
            <li class="<%= 'active' if action_name.eql? 'show' %>"><%= link_to t('navbar.mock.detail'), mock_path(@mock) %></li>
            <li class="<%= 'active' if action_name.eql? 'history' %>"><%= link_to t('navbar.mock.history'), history_mock_path(@mock) %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>