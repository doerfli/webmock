version: '2'
services:
  mongo:
    image: mongo:latest
    volumes:
      - /tmp/db:/data/db
  webmock:
    build: .
    command: bundle exec rails server
    #volumes:
    #  - .:/home/rails/webapp
    ports:
      - "3000:3000"
    #depends_on:
    #  - mongo
    links:
      - mongo:mongo

